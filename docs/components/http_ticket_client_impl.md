# HTTP Ticket Client Implementation

HTTP implementation of `TicketClient` using `httpx`.

## Overview

Talks to Team 3's issue tracker service over HTTP. Supports get, create, and update operations.

## Configuration

- `TICKET_SERVICE_BASE_URL` — base URL of the ticket service
- `TICKET_BOARD_ID` — board identifier
