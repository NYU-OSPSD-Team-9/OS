[project]
name = "ticket-client-api"
version = "0.1.0"
requires-python = ">=3.12"
description = "Shared ABC for issue tracker verticals (Teams 1, 3, 7)"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ticket_client_api"]
